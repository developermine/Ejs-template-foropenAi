const express = require('express');
const appController = require('../Controllers/appController');

const router = express.Router();


router.get('/', appController.ai_index);
router.post('/', appController.ai_post);

      

module.exports = router;